/*
** EPITECH PROJECT, 2019
** check player valid
** File description:
** checks if the player is allowed to jump
*/

#include "ggj.h"
/*
int check_player_valid(player *pl, platform *platforms)
{
    platform *elem = platforms;

    for(; elem != NULL; elem = elem->next) {
        if (((pl->vector.x > elem->x && (pl->vector.x < elem->x + 160)) ||
             ((pl->vector.x + 100 > elem->x && pl->vector.x + 100 < elem->x + 160) &&
              (elem->y - (pl->vector.y + 100) < 5))))
            return (1);
    }
    return (0);
    if (((pl->vector.x > 0 && (pl->vector.x < elem->x + 160)) ||
         ((pl->vector.x + 100 > elem->x && pl->vector.x + 100 < elem->x + 160) &&
          (elem->y - (pl->vector.y + 100) < 5))))
        return (1);
    if (((pl->vector.x > elem->x && (pl->vector.x < elem->x + 160)) ||
         ((pl->vector.x + 100 > elem->x && pl->vector.x + 100 < elem->x + 160) &&
          (elem->y - (pl->vector.y + 100) < 5))))
        return (1);
    if (((pl->vector.x > elem->x && (pl->vector.x < elem->x + 160)) ||
         ((pl->vector.x + 100 > elem->x && pl->vector.x + 100 < elem->x + 160) &&
          (elem->y - (pl->vector.y + 100) < 5))))
        return (1);
    return (0);
}

*/
